# install / build artifacts
node_modules/
bin/
package-lock.json

# per-stack config + secrets (generated by `pulumi stack init` / `pulumi config set`).
# The project file Pulumi.yaml IS tracked; per-stack Pulumi.<stack>.yaml files are not.
Pulumi.*.yaml
!Pulumi.yaml

# local
*.log
.env
